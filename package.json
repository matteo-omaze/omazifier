{
  "name": "omazifier",
  "version": "0.1.0",
  "description": "Composer engine: combines market-agnostic components into per-market trees from composition files. Framework-agnostic — no React, no data fetching, no translations, no shell.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test src/validate.test.ts src/resolve.test.ts src/authoring.test.ts src/compose.test.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2"
  }
}
